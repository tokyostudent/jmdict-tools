///<reference path="WordSuggest/Module.ts"/>

window.onload = () => {
/*
    var el = document.getElementById('content');
    var greeter = new Greeter(el);
    greeter.start();
*/

    var wordSuggest = new WordSuggest.Box($("#suggestBox"));


};